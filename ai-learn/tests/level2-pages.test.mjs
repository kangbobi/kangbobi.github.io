import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';const files=['01-neuron.html','02-activations.html','03-forward-pass.html','04-loss-gradient.html','05-backprop.html','06-tensors.html','07-autograd.html','08-training.html','09-failure-modes.html','10-capstone.html'];test('level 2 publishes ten lesson pages with teaching contract hooks',()=>{const dir=path.resolve(new URL('../level-2',import.meta.url).pathname);assert.equal(files.length,10);for(const file of files){const html=fs.readFileSync(path.join(dir,file),'utf8');assert.match(html,/ANALOGI/);assert.match(html,/INTERACTIVE LAB/);assert.match(html,/MENTAL MODEL/);assert.match(html,/FAILURE MODE/);assert.match(html,/Checkpoint/);assert.match(html,/level-2\.mjs/)}});