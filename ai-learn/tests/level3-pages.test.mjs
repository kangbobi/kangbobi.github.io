import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';const files=['01-language-modeling.html','02-tokenization.html','03-embeddings.html','04-position.html','05-attention-intuition.html','06-attention-math.html','07-causal-multihead.html','08-transformer-block.html','09-gpt.html','10-pretraining.html','11-decoding.html','12-finetuning.html','13-alignment.html','14-capstone.html'];test('level 3 publishes fourteen lesson pages with teaching hooks',()=>{const dir=path.resolve(new URL('../level-3',import.meta.url).pathname);assert.equal(files.length,14);for(const file of files){const html=fs.readFileSync(path.join(dir,file),'utf8');for(const hook of ['ANALOGI','INTERACTIVE LAB','MENTAL MODEL','FAILURE MODE','Checkpoint'])assert.match(html,new RegExp(hook));assert.match(html,/level-3\.mjs/)}});