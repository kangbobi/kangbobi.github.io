import {mountLessonShell} from '../core/lesson-shell.mjs';import {node,clear} from '../core/dom.mjs';import {simpleTokenize,cosineSimilarity,attentionWeights,causalMask,softmax,temperatureScale,topK,crossEntropy,bigramNext} from './logic.mjs';const id=document.body.dataset.module;mountLessonShell({levelId:'level-3',moduleId:id});const root=document.getElementById('labRoot'),explain=document.getElementById('labExplain');const cfg={
'01':[['context','model','logits','next token'],'Next-token prediction bukan database lookup; output adalah distribution yang dipelajari.','Mengapa language model bisa menghasilkan banyak token?','Karena token yang terpilih ditambahkan ke context lalu proses diulang.'],
'02':[['text','tokens','IDs','embeddings'],'Toy tokenizer di lab bukan implementasi BPE produksi.','Apakah token selalu sama dengan satu kata?','Tidak. Token dapat berupa bagian kata, karakter tertentu, atau simbol.'],
'03':[['token ID','embedding lookup','vector','network'],'Kedekatan toy vector bukan bukti makna kausal atau fakta dunia.','Kenapa ID 100 tidak “lebih besar maknanya” dari ID 5?','ID hanya indeks; representasinya ada di embedding vector.'],
'04':[['token embedding','+ position','ordered representation'],'Tanpa positional information, urutan sequence tidak terwakili dengan cara yang dibutuhkan model.','Apa yang berubah saat token sama pindah posisi?','Representasi inputnya karena komponen posisi berbeda.'],
'05':[['Q asks','K matches','softmax weights','V contributes'],'Attention weight tinggi menunjukkan kontribusi dalam komputasi, bukan penjelasan kausal lengkap.','Apa peran value V?','Informasi yang dijumlahkan menggunakan attention weights.'],
'06':[['Q·K','scale','softmax','weighted V'],'Softmax membuat relative weighting; score mentah bukan probabilitas.','Kenapa score dibagi √d?','Untuk mengendalikan skala dot product agar softmax lebih stabil.'],
'07':[['past tokens','causal mask','allowed attention','multiple heads'],'Causal mask mencegah leakage saat next-token training; menghapusnya mengubah objective.','Bolehkah token posisi 2 melihat posisi 5?','Tidak pada causal self-attention.'],
'08':[['norm','attention','residual','norm','MLP','residual'],'Residual bukan shortcut yang boleh dihapus tanpa mengubah optimization/data flow.','Apa fungsi residual connection secara sederhana?','Menjaga jalur informasi dan membantu optimization pada network dalam.'],
'09':[['IDs','embeddings','N blocks','norm','LM head','logits'],'Lebih banyak parameter tidak otomatis menjamin kualitas jika data/training buruk.','LM head menghasilkan apa?','Satu score/logit untuk setiap token vocabulary pada tiap posisi.'],
'10':[['token windows','forward','cross entropy','backward','update'],'Training loss turun belum membuktikan model berguna atau aman; perlu validation/evaluation.','Target pretraining causal LM apa?','Token berikutnya untuk setiap posisi yang diprediksi.'],
'11':[['logits','temperature','filter','sample/select','token'],'Temperature tidak menambah knowledge; hanya mengubah distribution decoding.','Apa efek temperature lebih rendah?','Distribution lebih tajam sehingga output cenderung lebih deterministik.'],
'12':[['pretrained model','task/instruction data','loss','updated behavior'],'Fine-tuning data buruk dapat menurunkan kemampuan atau mengajarkan pola yang tidak diinginkan.','Apakah fine-tuning mengganti tokenizer otomatis?','Tidak. Tokenizer biasanya tetap kecuali desain secara eksplisit mengubahnya.'],
'13':[['candidate behaviors','preference signal','optimization','policy behavior'],'Preference optimization tidak menjamin semua tujuan safety/quality terpecahkan.','Alignment terjadi sebelum atau sesudah base pretraining?','Biasanya sebagai tahap setelah base capabilities sudah dipelajari.'],
'14':[['text','tokens','embeddings','attention','blocks','logits','decode'],'Toy trace menjelaskan mekanisme, bukan mereplikasi ukuran atau perilaku model produksi.','Bagian mana memilih token akhir?','Decoding policy yang bekerja pada probability/logit output model.']};
function slider(label,min,max,step,value,on){const row=node('label',{className:'lab-row'}),n=node('span',{text:label}),input=node('input',{attrs:{type:'range',min,max,step,value}}),v=node('strong',{text:value});input.addEventListener('input',()=>{v.textContent=input.value;on(Number(input.value))});row.append(n,input,v);root.append(row);return input}function tokens(items){const box=node('div',{className:'tokens'});items.forEach(x=>box.append(node('span',{className:'token',text:x})));root.append(box)}function probs(labels,p){const box=node('div',{className:'probs'});labels.forEach((x,i)=>box.append(node('span',{className:'prob',text:`${x}: ${(p[i]*100).toFixed(1)}%`})));root.append(box)}function render(){clear(root);if(id==='01'){const seq=['model','belajar','dari','data','model','belajar','pola'];tokens(seq);const out=node('div',{className:'big'});root.append(out);const show=word=>{const r=bigramNext(seq,word);out.textContent=r.length?`${word} → ${r.map(([t,p])=>`${t} ${(p*100).toFixed(0)}%`).join(', ')}`:'tidak ada contoh next token'};const b=node('button',{text:'Coba context “model”'});b.onclick=()=>show('model');root.append(b);show('model');explain.textContent='Bigram ini model sangat kecil untuk memperlihatkan objective next-token.'}else if(id==='02'){const area=node('textarea',{attrs:{rows:3}});area.value='AI belajar dari data!';root.append(area);const out=node('div',{className:'tokens'});root.append(out);const update=()=>{clear(out);simpleTokenize(area.value).forEach((t,i)=>out.append(node('span',{className:'token',text:`${i}: ${t}`}))};area.addEventListener('input',update);update();explain.textContent='Tokenizer pedagogis berbasis pola Unicode, bukan tokenizer model komersial.'}else if(id==='03'){let x=.8,y=.2;const out=node('div',{className:'big'});root.append(out);const update=()=>out.textContent=`cosine([${x.toFixed(1)},${y.toFixed(1)}],[1,0]) = ${cosineSimilarity([x,y],[1,0]).toFixed(3)}`;slider('vector x',-1,1,.1,x,v=>{x=v;update()});slider('vector y',-1,1,.1,y,v=>{y=v;update()});update()}else if(id==='04'){let pos=0;const out=node('div',{className:'big'});root.append(out);const update=()=>out.textContent=`same token + position ${pos} → toy vector [${(1+pos*.1).toFixed(1)}, ${(0.5-pos*.03).toFixed(2)}]`;slider('position',0,12,1,pos,v=>{pos=v;update()});update()}else if(id==='05'||id==='06'){let q1=1,q2=0;const out=node('div');root.append(out);const update=()=>{clear(out);const w=attentionWeights([q1,q2],[[1,0],[.5,.5],[0,1]]);probsInto(out,['token A','token B','token C'],w)};const probsInto=(box,labels,p)=>labels.forEach((x,i)=>box.append(node('span',{className:'prob',text:`${x}: ${(p[i]*100).toFixed(1)}%`})));slider('query dim 1',-2,2,.1,q1,v=>{q1=v;update()});slider('query dim 2',-2,2,.1,q2,v=>{q2=v;update()});update()}else if(id==='07'){let size=5;const box=node('div',{className:'heatmap'});root.append(box);const draw=()=>{clear(box);causalMask(size).forEach((r,i)=>{const row=node('div',{className:'heat-row'});r.forEach((v,j)=>row.append(node('span',{className:'heat',text:v===-Infinity?'BLOCK':'ALLOW',attrs:{title:`query ${i} key ${j}`}})));box.append(row)})};slider('sequence length',2,7,1,size,v=>{size=v;draw()});draw()}else if(id==='08'||id==='09'){const stages=cfg[id][0];tokens(stages);explain.textContent=id==='08'?'Klik konsep di mental model: block mempertahankan shape sequence × d_model.':'Forward pass menghasilkan logits untuk vocabulary, belum menjadi teks sampai decoding.'}else if(id==='10'){let p=.55;const out=node('div',{className:'big'});root.append(out);const update=()=>out.textContent=`target probability ${p.toFixed(2)} → cross entropy ${crossEntropy([p,1-p],0).toFixed(3)}`;slider('target probability',.05,.99,.01,p,v=>{p=v;update()});update();explain.textContent='Semakin tinggi probability target benar, semakin rendah loss.'}else if(id==='11'){let temp=1;const labels=['data','server','model','kopi'],logits=[3,2.2,1.5,.5],out=node('div');root.append(out);const update=()=>{clear(out);const p=temperatureScale(logits,temp);labels.forEach((x,i)=>out.append(node('span',{className:'prob',text:`${x}: ${(p[i]*100).toFixed(1)}%`}));const top=topK(logits,2);explain.textContent=`Top-2 kandidat menurut logit: ${top.indices.map(i=>labels[i]).join(', ')}.`};slider('temperature',.2,2,.1,temp,v=>{temp=v;update()});update()}else if(id==='12'||id==='13'){const a=node('div',{className:'tokens'});['Base behavior','Task/preference examples','Updated behavior'].forEach(x=>a.append(node('span',{className:'token',text:x})));root.append(a);explain.textContent=id==='12'?'Objective dan format data menentukan perilaku yang ditekankan saat fine-tuning.':'Preference stage membandingkan perilaku yang lebih/kurang disukai; ini overview, bukan training RL di browser.'}else{const prompt=node('textarea',{attrs:{rows:2}});prompt.value='capacity power naik';root.append(prompt);const out=node('div');root.append(out);const update=()=>{clear(out);const ts=simpleTokenize(prompt.value);tokensInto(out,ts);const toy=temperatureScale([2.5,1.7,.8],.8);['aman','perlu','cek'].forEach((x,i)=>out.append(node('span',{className:'prob',text:`next ${x}: ${(toy[i]*100).toFixed(1)}%`}));explain.textContent='Trace toy: tokenizer + fixed illustrative logits. Tidak menjalankan model GPT sungguhan.'};const tokensInto=(box,ts)=>ts.forEach(t=>box.append(node('span',{className:'token',text:t})));prompt.addEventListener('input',update);update()}}
const c=cfg[id];c[0].forEach(x=>document.getElementById('mentalFlow').append(node('span',{text:x})));document.getElementById('failureText').textContent=c[1];document.getElementById('checkpointText').textContent=c[2];document.getElementById('checkpointAnswer').textContent=c[3];document.getElementById('revealCheckpoint').onclick=()=>document.getElementById('checkpointAnswer').hidden=false;document.querySelectorAll('[data-copy]').forEach(b=>b.onclick=async()=>{try{await navigator.clipboard.writeText(document.getElementById(b.dataset.copy).innerText);b.textContent='Copied'}catch{b.textContent='Copy gagal'}});render();